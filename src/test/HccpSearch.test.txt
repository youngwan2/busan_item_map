/** import { screen, render } from "@testing-library/react";
import { BrowserRouter } from "react-router-dom";
import HccpSearch from "../../components/page/HccpSearch";
import userEvent from "@testing-library/user-event";

test.skip("각 아이템을 클릭하면 모달창이 활성화된다", async() => {
  // const user = userEvent.setup();
  render(
    <BrowserRouter>
      <HccpSearch />
    </BrowserRouter>
  );
  const itemFigureEl = screen.getByRole("figure");
  const modalState = screen.getByText("false");
  await user.click(itemFigureEl);

  expect(modalState).toBe("true");
});

*/